@echo off
echo start of POSTBUILD.BAT

echo build type = %build_type%
echo compiling: %compilin_bname%

echo end of POSTBUILD.BAT
 
rem        return value must be zero
rem        otherwise build is stopped
exit 0

